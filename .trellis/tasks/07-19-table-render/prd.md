# Markdown 表格渲染支持

## Goal

博客文章能正确渲染 Markdown 表格（GFM 表格语法），并有合理的样式。

## Requirements

- 安装 `remark-gfm` 插件并在构建管道中启用
- 表格要有清晰的样式（边框、斑马纹、对齐）
- 移动端响应式（横向滚动）

## 验收标准

- [ ] Obsidian 中写的 `|` 表格正确转为 HTML `<table>`
- [ ] 表格有边框、表头、斑马纹样式
- [ ] 移动端窄屏可横向滚动
