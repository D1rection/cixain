---
title: ML1-9 高斯判别分析 GDA
date: 2026-08-27
updated: 2026-08-27
description: CS229学习：监督学习之生成学习
category: Tech
tags:
  - CS229
  - 机器学习
draft: true
---
## 1. 判别学习与生成学习

到目前为止，我们主要讨论的是对 $p(y|x;\theta)$ 建模的学习算法，即给定 $x$ 的 $y$ 的条件分布。例如，逻辑回归将 $p(y|x;\theta)$ 建模为 $h_\theta(x)=g(\theta^T x)$，其中 $g$ 是 Sigmoid 函数。我们将讨论一种不同类型的学习算法。

>考虑一个分类问题，我们希望根据动物的某些特征学习区分大象（$y=1$）和狗（$y=0$）。
>
>给定一个训练集，像逻辑回归或感知器算法（基本上）试图找到一条直线——即一个决策边界——来分隔大象和狗。然后，为了将一个新动物分类为大象或狗，它检查该动物落在决策边界的哪一侧，并据此做出预测。

这里有一个不同的方法。首先，看看大象，我们可以建立一个关于大象外观的模型。然后，看看狗，我们可以建立一个关于狗外观的独立模型。最后，为了分类一个新动物，我们可以将新动物与大象模型匹配，再与狗模型匹配，看新动物更像大象还是更像我们在训练集中看到的狗。

- 判别学习：直接学习 $p(y|x)$ 的算法（如逻辑回归）或者直接从输入空间 $\mathcal{X}$ 到标签 $\{0, 1\}$ 学习映射的算法（如感知器算法）
- 生成学习：对 $p(x|y)$ （和 $p(y)$） 进行建模

> 例如，如果 $y$ 表示一个样本是狗（$0$）还是大象（$1$），那么 $p(x|y=0)$ 对狗的特征分布建模，$p(x|y=1)$ 对大象的特征分布建模。

在建模 $p(y)$（称为类先验）和 $p(x|y)$ 之后，我们的算法可以使用贝叶斯规则推导给定 $x$ 的 $y$ 的后验分布： $$ p(y|x) = \frac{p(x|y)p(y)}{p(x)} $$ 其中分母由 $p(x) = p(x|y=1)p(y=1)+p(x|y=0)p(y=0)$ 给出，因此也可以用我们已学习的量 $p(x|y)$ 和 $p(y)$ 来表达。实际上，如果我们要计算 $p(y|x)$ 以进行预测，那么我们实际上不需要计算分母，因为（$y$ 并不影响 $p(x)$）： 

$$ \arg\max_{y} p(y|x) = \arg\max_{y} \frac{p(x|y)p(y)}{p(x)} = \arg\max_{y} p(x|y)p(y) $$

## 2. 高斯判别分布

### 2.1 多元高斯分布

多元高斯分布（也称为多元正态分布）是高斯分布从一维随机变量到 n 维随机变量（或简称为 n-随机变量）的推广。换句话说，多元高斯寻求对多个随机变量建模，而不是单变量随机变量。

假设 $X$ 分布为多元高斯分布，即 $X \in \mathbb{R}^{n}$，由均值向量 $\mu \in \mathbb{R}^{n}$ 和协方差矩阵 $\Sigma \in \mathbb{R}^{n \times n}$ 参数化，其中 $\Sigma \succeq 0$ 是对称且正的半定的。形式上，这可以写成

$$\mathcal{N}(\mu, \Sigma)$$

其密度由下式给出（$x$ 在这里是个向量）：

$$p(x; \mu, \Sigma) = \frac{1}{(2\pi)^{d/2} |\Sigma|^{1/2}} \exp\left( -\frac{1}{2} (x - \mu)^T \Sigma^{-1} (x - \mu) \right)$$

> 可以对比一维情形：$f(x) = \frac{1}{\sqrt{2\pi}\sigma} \exp\left(-\frac12 \frac{(x-\mu)^2}{\sigma^2}\right)$

## 参考

1. CS229 课程讲义
   https://cs229.stanford.edu/main_notes.pdf
2. https://aman.ai/cs229/gda/