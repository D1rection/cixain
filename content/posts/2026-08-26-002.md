---
title: LC231 2 的幂
date: 2026-08-26
updated: 2026-08-26
description: leetcode 231 题解
category: Soln
tags:
  - LeetCode
  - 位运算
source: LeetCode
difficulty: Easy
url: https://leetcode.cn/problems/power-of-two
draft: false

---
> [!fold] 题目描述
> 
> 给你一个整数 `n`，请你判断该整数是否是 2 的幂次方。
> 
> 如果是，返回 `true` ；否则，返回 `false`。 
> 如果存在一个整数 `x` 使得 `n == 2ˣ`，则认为 `n` 是 2 的幂次方。 
> 
> **示例 1：** 
> 输入：`n = 1` 
> 输出：`true` 
> 解释：`2⁰ = 1` 
> 
> **示例 2：** 
> 输入：`n = 16` 
> 输出：`true` 
> 解释：`2⁴ = 16` 
> 
> **示例 3：** 
> 输入：`n = 3` 
> 输出：`false` 

## 解法一：除法

比较常见的思路，通过除法，不断的将 `n` 除以 2，直到 `n` 无法被 2 整除时，看看最后这个约数到底是不是 1 即可。

```c++
class Solution {
public:
    bool isPowerOfTwo(int n) {
        if(n == 0) return n;
        while(n % 2 == 0) {
            n >>= 1;
        }
        return n == 1;
    }
};
```

## 解法二：位运算

二的幂的二进制特征是只有某一位为 `1` ，形如 `0..1..0`。并且负数不可能是二的幂。所以采用[[2026-08-26-001#^c800f1|位运算技巧]]来判断 `n` 的二进制中 1 的个数即可。

```c++
class Solution {
public:
    bool isPowerOfTwo(int n) {
        return (n > 0) && ((n & (n - 1)) == 0);
    }
}; 
```